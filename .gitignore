# ---------------------------------------------------------------- 저작물
# 포켓몬 도트/이미지는 닌텐도·게임프리크·크리처스의 저작물이다.
# 공개 저장소에 올리지 않는다. 필요할 때 서버가 받아서 캐시한다.
server/data/sprites/
client/sprites/
assets/sprites/
*.gif
!docs/*.gif
battle_shot*.png
anim_strip*.png
sprite_*.png
fx_*.png
battle_strip*.png

# 도감 원본 CSV 캐시 (tools/build_pokedex.py 가 다시 받는다)
tools/_cache/

# ---------------------------------------------------------------- 빌드 산출물
# exe 는 깃에 넣지 않는다. GitHub 릴리스에 올린다.
build/
dist/
*.spec

# ---------------------------------------------------------------- 파이썬
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# ---------------------------------------------------------------- 로컬
*.db
*.db-wal
*.db-shm
.env
*.log
*.part
.vscode/
.idea/
.DS_Store
Thumbs.db
ui_*.png
dlg_*.png
tools/_cache/
walk_*.png
icon_preview.png
shot_*.png

# ---------------------------------------------------------------- 백업
# 떠낸 DB 에는 닉네임과 비밀번호 해시가 들어 있다. 이 저장소는
# 공개라 한 번 올라가면 지워도 기록에 남는다. 아예 들어오지 못하게 막는다.
*.sql
*.sql.enc
